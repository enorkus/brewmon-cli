export class Gravity {
    timestamp: number
    name: string
    value: string

    constructor(timestamp: number, name: string, value: string) {
        this.timestamp = timestamp
        this.name = name
        this.value = value
    }
}
